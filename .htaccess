Options -Indexes
RewriteEngine On

# Route /en and /en/ to the English page
RewriteRule ^en/?$ en/index.php [L]

# Route everything else to the Japanese homepage
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^$ index.php [L]
